const {Router} = require('express');
const router = Router();

router.get(
    '/task',
    [],
    async (request, response) => {

    }
);

router.post(
    '/task',
    [],
    async (request, response) => {

    }
);

module.exports = router;
